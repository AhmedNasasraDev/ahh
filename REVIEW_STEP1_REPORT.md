# מחברת מתכונים — דוח בדיקה, שלב 1

**מה נעשה:** חילוץ ה-ZIP, סריקת כל הקוד, הרצה בפועל בדפדפן, בדיקת המנוע החישובי בנפרד, ו-Gap Analysis מול `RECIPE_NOTEBOOK_IMPLEMENTATION_SPEC.md`.
**מה לא נעשה:** אף שינוי בקוד. לא נמחק דבר, לא נוסף dependency, לא נעשה redesign, לא נוגע ב-backend.

---

## 1. מה נמצא בתוך ה-ZIP

12 קבצים, 437KB לא דחוס. בדיקת שלמות (`unzip -t`) עברה נקי — אין קבצים פגומים או חסרים ביחס לארכיון.

| קובץ | שורות | מה זה |
|---|---|---|
| `מחברת מתכונים.dc.html` | 2,729 | האפליקציה כולה — 22 מסכים + כל הלוגיקה של התצוגה |
| `support.js` | 1,911 | ה-runtime של Claude Design (`dc-runtime` מקומפל). **לא חלק מהמוצר** |
| `image-slot.js` | 1,225 | רכיב גרירת תמונות. **לא חלק מהמוצר** |
| `ios-frame.jsx` | 352 | מסגרת iPhone לתצוגה. **לא חלק מהמוצר** |
| `RECIPE_NOTEBOOK_IMPLEMENTATION_SPEC.md` | 659 | **המפרט — נמצא ונקרא במלואו** |
| `README.md` | 131 | מבוא + design tokens |
| `engine.js` | 137 | מנוע חישוב: עלות, תשואה, פחת, הידרציה, טמפ' מים, אלרגנים, תת-מתכונים |
| `measure.js` | 297 | יחידות, כלי מדידה, צפיפויות, כיול אישי, המרות, תבניות |
| `parser.js` | 93 | פענוח מתכון מטקסט חופשי |
| `groups.js` | 103 | Mock של קבוצות, קורסים, תפקידים, הרשאות |
| `learn.js` | 88 | כרטיסי מצב לימוד |
| `data.js` | 170 | 5 מתכוני דמו, קטגוריות, אנשי קשר |

### מה חסר בארכיון
- **תיקיית `images/` לא קיימת** — ה-README מבטיח `images/` עם placeholders. בהרצה יש 12 בקשות 404 (`images/cat-0..6.png`, `images/r-*.png`).
- אין `package.json`, אין `node_modules`, אין build config, אין בדיקות, אין `.git`, אין `.env`.
- שמות הקבצים בארכיון נשמרו ב-cp437 ולא ב-UTF-8 (חוץ מה-HTML). `unzip` רגיל יוצר שם קובץ מקולקל; נדרש חילוץ שמטפל בקידוד. זה כבר טופל.

---

## 2. באיזו טכנולוגיה הפרויקט בנוי

**חשוב להבין את זה קודם כל: זה לא פרויקט אפליקציה. זה מסמך עיצוב אינטראקטיבי.**

- **פורמט:** `.dc.html` — Claude Design Canvas. יש בו תגיות משלו: `<x-dc>`, `<sc-if>`, `<sc-for>`, `<x-import>`, ותבניות `{{ }}`.
- **מי מריץ את זה:** `support.js` — runtime שמפרש את התגיות האלה. הוא מוריד בזמן ריצה React 18.3.1, ReactDOM ו-Babel Standalone מ-`unpkg.com` (עם בדיקת SRI).
- **הלוגיקה:** `<script type="text/x-dc">` — הדפדפן לא מריץ אותו ישירות. בתוכו `class Component extends DCLogic` עם `state`/`setState` (דמוי React) ומתודה אחת `renderVals()` באורך ~1,160 שורות שמחשבת כל ערך שמופיע על המסך.
- **Framework:** אין. **Package manager:** אין. **Bundler:** אין. **TypeScript:** לא.
- **עיצוב:** אין שום CSS class. כל הסגנון inline כמחרוזות. פונטים מ-Google Fonts.
- **RTL:** `dir="rtl"` על השורש, `tabular-nums` בכל מקום, `direction:ltr` נקודתי על מספרים. mobile-first במסגרת קבועה 402×874.

**המשמעות המעשית:** אי אפשר "להמשיך לפתח" את הקובץ הזה. חייבים לשחזר אותו בסביבת אפליקציה אמיתית. זה בדיוק מה שה-README וה-SPEC אומרים, ואני מסכים איתם.

---

## 3. איך הפרויקט בנוי

```
מחברת מתכונים.dc.html
├── שורות 1–37     head: פונטים, CSS להדפסה, טעינת 7 מודולים
├── שורות 38–1564  Markup: 22 בלוקי <sc-if> — מסך אחד לכל בלוק
└── שורות 1566–2727 class Component
    ├── state (22 שדות: screen, recipes, cur, prefs, scaleMode, view, kDone, myNotes, groupPerms...)
    ├── componentDidMount — ממתין ל-4 מודולים (80 ניסיונות × 60ms), טוען מהאחסון
    ├── CCPS + haccpOf()  — נקודות בקרה HACCP
    ├── persistAll / setPrefs / pushVersion / versionDiff
    ├── doParseLocal / doParseSmart
    └── renderVals() — ~1,160 שורות: מחשבת הכול לכל המסכים בכל רינדור
```

הלוגיקה העסקית נמצאת בארבעה מודולים גלובליים נפרדים (`window.PN_ENGINE`, `PN_MEASURE`, `PN_PARSER`, `PN_GROUPS`) — וזה **הנכס האמיתי של הפרויקט**. הם נקיים מ-UI וניתנים להעברה כמעט as-is.

---

## 4. אילו מסכים קיימים

22 מסכים. ניווט לפי `state.screen`, tab bar תחתון: בית · מחברת · קבוצות · עוד.

| מסך | האם הרצתי ובדקתי בפועל |
|---|---|
| `onboarding` (3 צעדים) | ✅ נבדק |
| `home` | ✅ נבדק |
| `list` (מחברת) | ✅ נבדק |
| `recipe` | ✅ נבדק |
| `edit` | ✅ נבדק |
| `locked` (מסך נעילה) | ✅ נבדק |
| `kitchen` (Cook Mode) | ✅ נבדק |
| `batch` (אצוות + HACCP) | ✅ נבדק |
| `groups` | ✅ נבדק |
| `group` | ✅ נבדק (גם כתלמיד וגם כמדריך) |
| `groupRecipe` | ✅ נבדק |
| `perms` (הרשאות) | ✅ נבדק |
| `stock` (מלאי ומחירים) | ✅ נבדק |
| `plan` (יום ייצור) | ✅ נבדק |
| `tools` (כלי המדידה שלי) | ✅ נבדק |
| `settings` | ✅ נבדק |
| `more` | ✅ נבדק |
| גלית המרת יחידה | ✅ נבדק |
| גלית שיתוף | ✅ נבדק |
| `label` (תווית) | ⚠️ נבדק בקוד בלבד |
| `order` (דף הזמנה) | ⚠️ נבדק בקוד בלבד |
| `learn` (מצב לימוד) | ⚠️ נבדק בקוד בלבד |
| `diff` (השוואת גרסאות) | ⚠️ נבדק בקוד בלבד |
| `paste` (הדבקה) | ⚠️ הפענוח נבדק בקוד ובבדיקת יחידה |

---

## 5. איך הרצתי את הפרויקט

`unpkg.com` חסום במדיניות הרשת של הסביבה הזאת. לכן:
1. הורדתי מ-npm בדיוק את אותן גרסאות (react 18.3.1, react-dom 18.3.1, @babel/standalone 7.29.0).
2. הגשתי את התיקייה ב-`http-server`.
3. הרצתי Chromium דרך Playwright והפניתי את בקשות ה-CDN לקבצים המקומיים (request interception).
4. הזרקתי shim ל-`window.storage` (מגובה ב-localStorage) כדי לבדוק שמירה.

**אף קובץ בפרויקט לא שונה.** כל ההתאמות נעשו מחוץ לפרויקט.

**תוצאה:** האפליקציה עולה ורצה. **אפס שגיאות JavaScript. אפס `pageerror`.** השגיאות היחידות בקונסול הן 404 על תמונות חסרות ועל Google Fonts (חסום בסביבה). כל 22 המסכים מרונדרים.

---

## 6. אילו פיצ'רים באמת עובדים

כל מה שלהלן נבדק בפועל ומאומת במספרים.

| פיצ'ר | אימות |
|---|---|
| חישוב עלות, עלות לק"ג, עלות ליחידה, פוד קוסט | בריוש: 1,178 גר', עלות ₪19.20, ₪16.7 לק"ג, ₪1.61 ליחידה |
| תשואה תאורטית/מעשית + פחת ייצור + פחת אפייה | קרם פטיסייר: 1.55 ק"ג → 1.49 ק"ג, פחת ייצור 3.7% |
| הידרציה גולמית ונטו (לפי אחוז מים בכל נוזל) | בריוש: 67.4% גולמי, 52.0% מים בפועל |
| טמפרטורת מים (כלל שלוש הטמפרטורות) | בריוש: 17°C |
| זיהוי אלרגנים אוטומטי + ידני | בריוש: גלוטן / חלב / ביצים |
| תת-מתכונים רקורסיביים כולל גלגול עלות | בריוש שוקולד: 300 גר' גנאש = ₪13.67, מדויק לאגורה |
| הגנה מפני מעגל תת-מתכונים | לא קורס |
| המרת יחידות תלוית חומר גלם | 2 כוסות קמח → 240 גר' עם תג "נתון מערכת" |
| סירוב להמיר בלי נתון אמין | חומר לא מוכר → הסבר + הצעת כיול, **בלי מספר** |
| oz מול fl oz — רק דרך צפיפות | 10 oz קמח → 19.2 fl oz; בלי נתון → כשל מוסבר |
| כיול אישי דורס נתון מערכת | כיול 132 גר'/כוס → 264 גר' עם תג "כיול אישי" |
| סקיילינג (מנות/יחידות/משקל/מלאי) בלי דריסת המקור | מאומת |
| התאמת תבנית | Ø20 → Ø26 = מקדם **1.69**, בדיוק כמו ב-AC #15 |
| Cook Mode | סימון שלב → מתקדם אוטומטית, מונה 0/6 → 1/6, פס התקדמות |
| טיימרים מקבילים | טיימר 6 דק' רץ (5:57), עצירה/מחיקה |
| היסטוריית גרסאות | שינוי קמח 500→600 יצר V1 עם תיאור "שונתה כמות: קמח לחם 13% חלבון" + כפתור שחזור |
| מסך נעילה לנוסחה מאושרת | "בריוש נאנטר חסום לעריכה" + "ביטול נעילה ועריכה" |
| HACCP — סטטוס נגזר | 4/4 CCP + קירור 4°C → **"HACCP תקין"**; 4/4 + קירור 7°C → **"חריגה"** + התראה; 0 CCP → "לא תועד" |
| עדכון מחיר מרכזי | שינוי מחיר חמאה → "המחיר של חמאה 82% עודכן ב־4 מתכונים" |
| יום ייצור — לוח זמנים אחורה | אספקה 07:00 → התחלה 03:22, שעה לכל שלב |
| רשימת רכש מצטברת | 7 שורות, עלות סחורה ליום ₪19.2 |
| פענוח מתכון מקומי | "2 כוסות קמח" → 240 גר' + הערה "במקור 2 כוס"; "שעה וחצי" → 90 דק'; "180 מעלות" → 180 |
| הערות אישיות | נשמרות בנפרד מ-`notes`, עם debounce |
| Progressive Disclosure (פרופילים) | מצב פשוט/מקצועי מסתיר עלויות ותשואה |
| Onboarding + איפוס מההגדרות | מאומת |

---

## 7. אילו פיצ'רים הם רק Demo / Mock / UI

| אזור | המצב בפועל |
|---|---|
| Authentication | לא קיים כלל. אין חשבון, אין התחברות, אין זהות |
| Database | לא קיים. אין שרת, אין API, אין סנכרון |
| קבוצות | 2 קבוצות קבועות בקוד ב-`groups.js`. התפקיד שלי (`myRole`) כתוב בקוד |
| חברות | "18 חברים" זה מספר קבוע. אין רשימת חברים |
| הזמנות / קוד / קישור | "בקשת הצטרפות" מציגה הודעה בלבד. אין אימות קוד, אין תוקף |
| בקשות ממתינות | 2 בקשות קבועות. אי אפשר לאשר |
| הרשאות | המתגים משנים את המסך בלבד. אין אכיפה — המסך עצמו אומר זאת |
| מתכון קבוצתי | נשלף מהמחברת **האישית** של המשתמש, לא ממקור נפרד |
| שיתוף | מייצר קוד מקומי רנדומלי ולא שולח דבר |
| ייצוא JSON | מציג הודעה ולא מייצר קובץ |
| פענוח חכם (Claude API) | קורא ל-API מהדפדפן בלי מפתח. תמיד ייכשל |
| צילומי אצוות | נשמרים על המכשיר בלבד, בלי חותמת זמן מאומתת |
| ערבית | "بהכנה" בהגדרות |
| תמונות | placeholders שלא קיימים בארכיון |

---

## 8. איזה מנגנון שמירה קיים כרגע

**מפתח אחד, `pastry-notebook-v1`, JSON אחד, דרך `window.storage`.**
המבנה: `{ recipes, prefs, myNotes, groupPerms, saved }`.

**זו הבעיה המעשית מספר 1:**
`window.storage` הוא API שמספקת סביבת Claude Design/Artifacts. **הוא לא localStorage, והוא לא מסופק על ידי `support.js`.** בדקתי את זה: אין שום אזכור ל-`storage` ב-`support.js`.

מכאן: אם פותחים את הקובץ בדפדפן רגיל, `window.storage` הוא `undefined`. הקוד מוגן (`window.storage && ...`) ולכן **לא קורס** — אבל **שום דבר לא נשמר**. כל רענון מחזיר לנתוני הדמו. אימתתי שמירה רק אחרי שהזרקתי shim משלי.

בעיות נוספות באחסון:
- הכול blob אחד. שינוי של הערה אישית אחת כותב מחדש את כל המחברת.
- אין migration/versioning לסכימה. יש `migrate()` אבל הוא רק ממלא ברירות מחדל.
- אין הגנה מפני כתיבה מקבילה, אין גיבוי, אין ייצוא אמיתי.

---

## 9. האם יש Backend אמיתי

**לא. אין שום backend.** אין endpoint אחד, אין קריאת רשת אחת לשרת שלנו. הקריאה היחידה החוצה היא ל-`api.anthropic.com` — והיא שבורה (ראו §12).

## 10. האם יש Database אמיתי

**לא.** אין DB, אין ORM, אין schema, אין migrations. יש JSON אחד במכשיר. המפרט מגדיר 20 טבלאות — אף אחת מהן לא קיימת.

## 11. האם יש Authentication אמיתי

**לא.** אין מודל משתמש בכלל. `prefs` הוא אובייקט מקומי. מסך ההגדרות עצמו אומר: *"בגרסת העיצוב הזו אין עדיין חשבונות משתמש, והכול נשמר מקומית במכשיר."* — יישר כוח על הכנות הזאת.

## 12. האם קיימות קבוצות והרשאות אמיתיות

**הדמיה מלאה, לא אמיתי.** אבל ההדמיה עצמה מדויקת ומעוצבת נכון:
- ברירת המחדל להרשאות היא **המגבילה ביותר** — אימתתי: `{view:true, save:false, print:false, download:false, shareOut:false}`.
- גזירת תפקידים עובדת: תלמיד (g1) לא רואה כפתור הרשאות ולא רואה בקשות ממתינות. מדריך (g2) רואה את שניהם.
- מתכון עם `save:false` מציג הודעה מסבירה במקום כפתור השמירה — אימתתי.
- שמירת עותק למחברת עובדת ויוצרת עותק עם `savedFrom` ו-`versionNote` נכונים.
- מסך ההרשאות אומר במפורש: *"שינוי הרשאה כאן משנה את המסך בלבד. אכיפה אמיתית דורשת שרת והרשאות במסד הנתונים."*

**האזהרה:** ה-override של ההרשאות (`groupPerms`) נשמר ב-blob **של המשתמש עצמו**. כלומר בעולם האמיתי תלמיד יכול לשנות את ההרשאות של עצמו. האכיפה חייבת לעבור לשרת, ובמיוחד `save-copy`.

## 13. האם מנגנון המרת היחידות אמיתי

**אמיתי, והוא החלק הטוב ביותר בפרויקט — אבל יש בו ארבעה פגמים אמיתיים.**

מה שעובד נכון (אימתתי בבדיקות יחידה):
- סדר הקדימות מדויק לפי המפרט: `personal` → `recipe` → `system` → `estimate` → `null`.
- `null` הוא תשובה לגיטימית: חומר גלם לא מוכר מחזיר הסבר ולא מספר. זה בדיוק מה שמפריד בין כלי מקצועי לצעצוע.
- oz ו-fl oz לעולם לא מומרים ישירות.
- 25+ חומרי גלם בטבלה, עם סימון `system` מול `estimate`.

**הפגמים** (פירוט מלא ב-§17): טבלאות צפיפות מפוצלות, `engine.js` מתעלם מגודל הכוס שהמשתמש הגדיר, גלית ההמרה מקבלת את הרכיב כבר בגרמים ולכן מסמנת אומדן כ"מדויק", והתאמת הכיול לפי שם היא substring דו-כיווני.

## 14. האם Cook Mode עובד

**כן, במלואו.** אימתתי בהרצה: מסך כהה, שלב אחד, פס התקדמות לכל שלב וניתן ללחיצה, "סימון השלב כהושלם" מסמן ומתקדם אוטומטית, מונה "1 מתוך 6 שלבים הושלמו", טיימרים מקבילים שרצים באמת (ראיתי 5:57 יורד), עצירה/המשך/מחיקה, ניווט הקודם/הבא + "סיום ההכנה".

## 15. האם Version History עובד

**כן.** אימתתי: פתחתי נוסחה נעולה, שיניתי קמח מ-500 ל-600, שמרתי — נוצרה `V1` עם snapshot מלא ועם תיאור מחושב **"שונתה כמות: קמח לחם 13% חלבון"**, וכפתור "שחזור" זמין. השחזור בקוד דוחף את הגרסה הנוכחית להיסטוריה לפני החזרה — כלומר לא מוחק.

## 16. האם פרטיות המחברת נאכפת

**לא נאכפת — מוצגת בלבד. אבל ההפרדה בקוד נכונה, וזה חשוב.**

בדקתי: `privateNotes` מופיע **רק** במסך המתכון. הוא לא מופיע בדף ההזמנה, לא בתווית, לא בשיתוף ולא בייצוא הקבוצתי. הערה על מתכון קבוצתי נשמרת במפתח נפרד `myNotes["<groupId>:<itemId>"]` ולא נוגעת במתכון המקור.

כלומר: מודל הפרטיות מיושם נכון בשכבת הנתונים. מה שחסר זה **מי אוכף אותו** — בלי חשבונות ובלי RLS, אין מה לאכוף. ברגע שיהיה שרת, ההפרדה הזאת כבר מוכנה.

---

## 17. באגים ובעיות שמצאתי

מסודר לפי חשיפה. כל פריט מאומת.

### חמור

**B1 — `engine.js` מתעלם לגמרי מכלי המדידה של המשתמש.**
`toGrams(ing)` מקבל פרמטר אחד. אין לו גישה ל-`prefs`. כוס מקובעת ל-240 מ"ל ולטבלה `CUP_DRY` פנימית.
*אימות:* יצרתי מתכון עם "2 כוסות קמח" → 240 גר'. שיניתי בהגדרות כוס ל-250 מ"ל → **עדיין 240 גר'**.
*ההשלכה:* Acceptance Criterion #5 במפרט (*"שינוי כוס מ-240 ל-250 משנה את כל ההמרות מיד"*) **נכשל** בכל מה שעובר במנוע: טבלת הרכיבים, התשואה, העלויות, ההידרציה, דף ההזמנה, יום הייצור ורשימת הרכש. זה עובד **רק** בגלית ההמרה ובתצוגת "ביתי".

**B2 — שלוש טבלאות צפיפות עצמאיות שחולקות זו על זו.**
`engine.CUP_DRY`, `measure.TABLE` ו-`parser.DRY` — שלושה מקורות נפרדים.
*אימות:* קקאו — engine: 110 גר'/כוס · measure: 42 גר'/100 מ"ל = 100.8 גר'/כוס · parser: 110 גר'/כוס.
*ההשלכה:* אותו רכיב מקבל משקל שונה תלוי באיזה מסך אתה נמצא. וההוראה בהעברה *"להעביר את הטבלאות כמו שהן"* לא אומרת איזו מהן.

**B3 — גלית ההמרה מסמנת אומדן כ"המרה מדויקת".**
הגלית מקבלת את הרכיב כבר אחרי המרה לגרמים (`convIng.qty = row.g, unit:'גרם'`). היחידה המקורית אובדת.
*אימות:* רכיב "2 כוס קמח" → פתיחת הגלית → המרה לגרם → התוצאה "240 גר'" עם תג **"המרה מדויקת · המרת משקל למשקל היא יחס קבוע"**. אבל ה-240 עצמם באו מטבלת אומדן.
*ההשלכה:* AC #2 (*"מציג גרמים עם תג נתון מערכת"*) נכשל, וזה בדיוק סוג הביטחון המדומה שהמפרט אוסר.

**B4 — התאמת כיול אישי לפי substring דו-כיווני.**
`calibFor`: `n.includes(c.name) || c.name.includes(n)`.
*אימות:* כיול בשם "קמח" מוחל על "קמח שקדים" ומחזיר `source: 'personal'` — כלומר תג ירוק "כיול אישי · מדויק" על מספר שגוי לחלוטין (קמח שקדים צפוף בערך ב-20% פחות מקמח חיטה).
*ההשלכה:* הפיצ'ר שנועד לשפר דיוק יכול להזיק לו, ובביטחון מלא.

### בינוני

**B5 — כיול לא שומר את נפח הכלי שבו נמדד.** נשמר `{name, tool, grams}` והצפיפות מחושבת מחדש מ-`prefs.tools` הנוכחי. שינוי גודל הכוס משנה בשקט כיולים מהעבר (132 גר'/כוס: 55 → 52.8 גר'/100 מ"ל). ההערה ב-`measure.js` עצמו מתעדת שדה `ml` שלא נכתב לעולם.

**B6 — `panFactor` משווה תבניות לא-תואמות.** מאפינס (12 שקעים) מול Ø20 → מקדם **26.18**, ומוצג כהמלצה. "שטח" של מאפינס הוא מספר השקעים, לא סמ"ר.

**B7 — ביטול נעילה הוא חד-כיווני.** `startEdit(true)` קובע `locked: false`, ואין מתג `locked` בטופס העריכה. אחרי ביטול נעילה אחד, אי אפשר להחזיר מתכון למצב "נוסחה מאושרת" לעולם.

**B8 — שתי הודעות UI מציגות פעולת Mock כאילו התבצעה.**
*אימות:* ייצוא → *"ייצוא JSON — 6 מתכונים הועתקו לקובץ"* (לא נוצר קובץ). שיתוף → *"נשלח שיתוף חד פעמי ל־דנה לוי · קוד 0RNROA · הקישור נפתח פעם אחת ואז נסגר"* (לא נשלח דבר, אין קישור).
זה מפר את AC #17 של המפרט עצמו: *"אין שום מסך שבו פעולת Mock מוצגת כאילו היא מחוברת לשרת."* שאר המסכים כן עומדים בזה.

**B9 — תמונות שבורות.** תיקיית `images/` חסרה (12 × 404). בנוסף, ה-`image-slot` שולח בקשות למחרוזות התבנית עצמן לפני האינטרפולציה — `{{ r.img }}`, `{{ heroImg }}`, `{{ t.img }}`, `{{ s.img }}`, `{{ lastImg }}` — עוד 404 ורעש בקונסול.

### קל

- **B10** — `fmt(2,'egg')` מחזיר **"2 ביצהות"**. ריבוי שבור בעברית. `fmt(0.1,'cup')` מחזיר "0 כוס" במקום "פחות מרבע כוס".
- **B11** — עיגול כף/כפית לרבעים; המפרט §5.2 אומר חצאים. יש שם תנאי מת: `x.tool === 'cup' ? 0.25 : 0.25`.
- **B12** — מחיקת אצווה מסננת לפי `code`; קוד אוטומטי נגזר מ-`batches.length + 1`. אחרי מחיקה אפשר לקבל קודים כפולים, ומחיקה אחת תמחק את כולם.
- **B13** — `compute()` מחזיר `{error:'מעגל תת־מתכונים'}` אבל אין מסך שקורא את `error`. המעגל פשוט מתומחר ב-0 בשקט.
- **B14** — קטלוג חומרי הגלם מבוסס על **השוואת שמות מדויקת**. בנתוני הדמו עצמם: "חמאה 82%", "חמאה 82% ללישה" ו-"חמאת למינציה 84%" הן שורות נפרדות, וכך גם "קמח לחם 13% חלבון" מול "קמח לחם 12.5% חלבון". "מים" מופיע כחומר גלם שדורש מחיר ב-ש"ח לק"ג.
- **B15** — מתכון קבוצתי נשלף מ-`S.recipes` — המחברת האישית. עריכת הבריוש שלך משנה את מה ש"המתכון של הקבוצה" מציג, בניגוד ל-§11 (*"מתכון המקור של הקבוצה אינו משתנה לעולם"*).
- **B16** — יעדי ההמרה בגלית הם `g, kg, ml, l, cup, tbsp, tsp, oz, floz` — בלי יחידות ספירה. אי אפשר לשאול "כמה ביצים זה 275 גרם", למרות שהמנוע יודע.
- **B17** — אפשר להוסיף כמה טיימרים לאותו שלב בלחיצות חוזרות.

---

## 18. בעיות אבטחה

**S1 — קריאה ל-Claude API ישירות מהדפדפן, בלי מפתח.**
`doParseSmart` עושה `fetch('https://api.anthropic.com/v1/messages')` **בלי `x-api-key` ובלי `anthropic-version`**. בכל פריסה אמיתית זה יחזיר 401. הדרך היחידה "לתקן" את זה בצד לקוח היא להטמיע מפתח בדפדפן — **אסור בהחלט**. נדרש proxy בצד שרת עם rate limit פר משתמש. (המפרט אומר את זה, וטוב שכך.) בנוסף, מזהה המודל בקוד הוא `claude-sonnet-4-6` — צריך לעדכן לדגם עדכני כשבונים את ה-proxy.

**S2 — אין זהות בכלל.** blob אחד במכשיר. מי שמחזיק את המכשיר מחזיק הכול: מתכונים, מחירים, הערות אישיות, כיולים.

**S3 — הרשאות הן קוסמטיקה, וה-override בבעלות המשתמש הנבדק.** `groupPerms` נשמר ב-blob של המשתמש. בעולם אמיתי תלמיד היה יכול לשנות את ההרשאות של עצמו. `perm_save` חייב להיבדק בשרת, ו-`save-copy` חייב להיות endpoint בשרת.

**S4 — אין RLS.** כל מודל הפרטיות (§12 במפרט) הוא הבטחה בממשק. ההבטחה הקריטית — שמדריך לא רואה את המחברת האישית ואת ההערות של תלמיד — חייבת policy במסד, לא בדיקה בקוד הלקוח.

**S5 — תיעוד HACCP לא שווה כלום לביקורת.** הצילום על המכשיר, התאריך נגזר מ-`new Date()` בלקוח והמשתמש יכול להזין תאריך ידנית. חותמת זמן שהמשתמש יכול לערוך אינה תיעוד. זה גם מה שהמפרט אומר, ואני מדגיש: **אל תשווקו את זה כתיעוד HACCP עד שהחותמת מגיעה מהשרת והרשומה בלתי ניתנת לשינוי.**

**S6 — אין ולידציה על קלט מספרי.** טמפרטורות, משקלים, מחירים וכמויות מקבלים כל טקסט; `Number()` מחזיר 0 או NaN בשקט. כשיהיה DB זה יהפוך לבעיית שלמות נתונים.

**S7 — תלויות צד שלישי נטענות בזמן ריצה מ-CDN.** React, ReactDOM ו-**Babel Standalone** (קומפילציה בדפדפן). יש SRI, וזה תקין לפרוטוטייפ — אבל אסור ב-production. בפרויקט האמיתי הכול bundled.

**S8 — אין CSP, אין ניהול סודות, אין הנחות HTTPS.** מצופה בשלב הזה, אבל צריך להיות ברשימה.

---

## 19. מה חסר כדי להפוך את זה לאפליקציה אמיתית

לפי סדר תלות:

1. **החלטה על פלטפורמת יעד** — אין לזה תשובה בארכיון. חובה להכריע לפני שורת קוד אחת (ראו §20).
2. **פרויקט אמיתי** — repo, package manager, build, TypeScript, lint, CI.
3. **Auth + מודל משתמש** — אין כלום. ה-onboarding צריך לכתוב ל-`profiles`.
4. **Database + 20 טבלאות + RLS** — המפרט כבר כתב את הסכימה ואת ה-policies. זה נכס, לא צריך לתכנן מחדש.
5. **שכבת גישה לנתונים** — להחליף blob אחד בשאילתות. זה השינוי הארכיטקטוני הגדול.
6. **מנוע חישוב מאוחד** — טבלה אחת, פונקציה אחת, `prefs` כפרמטר (מתקן B1–B4 בבת אחת).
7. **סנכרון בין מכשירים** — כרגע מכשיר אחד, ואפילו זה לא נשמר בדפדפן רגיל.
8. **קטלוג חומרי גלם כישות** — במקום השוואת שמות (B14).
9. **Storage לתמונות** — bucket פרטי, גישה חתומה, המרה ל-WebP.
10. **Endpoints לקבוצות** — הזמנות, קוד, אישורים, ובמיוחד `save-copy` בשרת.
11. **Proxy לפענוח החכם** + rate limit + cache.
12. **HACCP עם חותמת זמן מהשרת** ורשומות בלתי ניתנות לעריכה.
13. **ייצוא/ייבוא אמיתי** (B8).
14. **תמונות אמיתיות** במקום ה-placeholders החסרים.
15. **i18n לערבית.**
16. **מערך בדיקות** — 30+ ה-acceptance criteria במפרט הם מפרט בדיקות מוכן. כרגע אין אף בדיקה.
17. **הדפסה אמיתית** — ה-CSS להדפסה חושף רק `#orderSheet`, כלומר רק דף ההזמנה מודפס.

---

## 20. סדר העבודה שאני ממליץ עליו

### קודם כל — שתי החלטות שאתה צריך לקבל

**החלטה 1: פלטפורמה.**
ה-README מציע "React Native, SwiftUI, React, וכו'". אלה לא אפשרויות שוות:
- **SwiftUI** — שכתוב מלא של 2,700 שורות layout, iOS בלבד, ואין לך שם שימוש חוזר במנוע ה-JS.
- **React Native** — גם שכתוב מלא של הסגנונות (RN לא מקבל `border-radius:999px` כמחרוזת, ואין לו `background` shorthand). המנוע כן עובר as-is.
- **React web (Vite + TypeScript) כ-PWA** — **זו ההמלצה שלי.** הסגנונות עוברים כמעט 1:1, RTL כבר פתור ב-CSS, המנוע עובר as-is, ו-Capacitor יכול לעטוף לאפליקציית חנות בהמשך אם צריך. הדרך הזולה והמהירה למוצר עובד.

**החלטה 2: היקף גרסה 1.**
22 מסכים ל-v1 זה יותר מדי, ואני אומר את זה בפירוש. הקבוצות והקורסים הם החלק היקר ביותר בפרויקט — הזמנות, קודים, אישורים, הרשאות פר-מתכון, RLS, `save-copy` בשרת — לפי הערכתי **בערך 40% מעבודת ה-backend**, והם משרתים קהל צר יותר מהליבה.
**ההמלצה שלי:** v1 = המחברת האישית שעובדת מצוין על כמה מכשירים. v2 = קבוצות.

### סדר העבודה

**שלב 0 — תשתית והכרעות (1–2 ימים)**
פרויקט Vite + React + TS, lint, CI, ו-**להעביר את `engine.js`/`measure.js`/`parser.js`/`learn.js` as-is** עם עטיפת טיפוסים. מיד אחר כך: לכתוב את 30 ה-acceptance criteria כבדיקות אוטומטיות. הן ייכשלו על B1–B4 — וזה בדיוק מה שאנחנו רוצים לראות.

**שלב 1 — איחוד מנוע החישוב (2–3 ימים) ⭐ הכי חשוב**
מקור אמת אחד לצפיפויות. `toGrams(ing, prefs)` במקום `toGrams(ing)`. גלית ההמרה מקבלת את היחידה המקורית ולא גרמים. התאמת כיול לפי התאמה מדויקת או בחירה מפורשת, לא substring.
**אני ממליץ לעשות את זה לפני כל UI.** זה מתקן את ארבעת הבאגים החמורים ביחד, וכל שאר המערכת נשענת על זה. אם נבנה UI קודם, נתקן את זה פעמיים.

**שלב 2 — Auth + פרופילים (2–3 ימים)**
**Supabase.** המפרט כתוב מילה במילה נגד Postgres עם `auth.uid()` — זו התאמה מושלמת, לא צריך לתכנן מחדש. onboarding כותב ל-`profiles`, זריעת `density_table`.

**שלב 3 — המחברת מול DB (5–7 ימים)**
`recipes`/`ingredients`/`steps` + RLS אישי + `recipe_versions` + restore + `private_notes` + `calibrations`. ההערות והכיולים הם **הבדיקה הראשונה של RLS** — בדיוק כמו שהמפרט ממליץ.

**שלב 4 — מסכי הליבה (7–10 ימים)**
בית, מחברת, מתכון, עריכה, Cook Mode, כלי מדידה, הגדרות, הדבקה. בדיוק לפי ה-tokens ב-§16.

**שלב 5 — כלים מקצועיים (4–6 ימים)**
יום ייצור, רכש, קטלוג חומרי גלם כישות אמיתית (B14), תווית, דף הזמנה, הדפסה שעובדת, מצב לימוד, השוואת גרסאות.

**— כאן v1 יכולה לצאת. עד לנקודה הזו יש מוצר שאפשר להשתמש בו באמת. —**

**שלב 6 — Storage לתמונות (2–3 ימים)**

**שלב 7 — קבוצות והרשאות (7–10 ימים)**
`groups`/`courses`/`lessons`/`group_recipe_items` + RLS + `save-copy` בשרת + הזמנות/קודים/אישורים. מתכון הקבוצה חייב להיות ישות נפרדת, לא הפניה למחברת האישית (B15).

**שלב 8 — Proxy לפענוח חכם (1–2 ימים)**
Edge function, מפתח בשרת, rate limit פר משתמש, cache. לשים לב לעלות.

**שלב 9 — HACCP אמיתי (3–4 ימים)**
`batch_photos`, חותמת זמן מהשרת, רשומות בלתי ניתנות לעריכה, נקודות בקרה פר-מטבח.

**שלב 10 — ערבית (2–3 ימים)**

### עלויות מוערכות (טווחים גסים, לא הצעת מחיר)
- Supabase: חינם לפיתוח, ~$25/חודש ב-Pro כשיש משתמשים אמיתיים.
- Vercel או Netlify: חינם לפיתוח, ~$20/חודש ב-Pro.
- Apple Developer: $99/שנה — **רק** אם הולכים לחנות. ב-PWA לא צריך.
- Claude API לפענוח: פענוח מתכון בודד הוא סדר גודל של אגורות. עם cache ו-rate limit זה זניח עד שיש נפח משמעותי.

### שני דברים שאני ממליץ לא לעשות
1. **לא לשכתב את `engine.js`/`measure.js`.** הטבלאות, אחוזי המים והנוסחאות שם הם הידע המקצועי של המוצר. לאחד — כן. לשכתב — לא.
2. **לא להתחיל מהקבוצות.** זה החלק הכי יקר, הכי מסוכן מבחינת אבטחה, והכי פחות דחוף.

---

## Gap Analysis מול RECIPE_NOTEBOOK_IMPLEMENTATION_SPEC.md

**הקובץ נמצא בארכיון (659 שורות) ונקרא במלואו.** אין ניחושים כאן — כל שורה בטבלה מתייחסת לסעיף במפרט.

### DONE — ממומש ועובד
| פיצ'ר | סעיף |
|---|---|
| Onboarding 3 צעדים + איפוס מההגדרות | §4 |
| שלושה פרופילים; אינם נועלים שום פיצ'ר | §3 |
| `touchedUnits` מונע דריסת יחידות בהחלפת פרופיל | §3 |
| סדר קדימות צפיפות, כולל `null` כתשובה לגיטימית | §5.1 |
| כל ששת סוגי ההמרה | §5.2 |
| oz ≠ fl oz — רק דרך צפיפות | §18.2 |
| כיול אישי דורס נתון מערכת | §5.1 |
| כיול כוס נגזר לכף ולכפית ביחס נפח | §18.4 |
| 4 מצבי סקיילינג; המקור לא נדרס | §6 |
| אזהרת חריגה מעל 5% מהיעד | §6, §18.12 |
| `panFactor` — נפח כששני הגבהים ידועים, אחרת שטח; סף 2% | §7 |
| הערות אישיות מופרדות מ-`notes`, לא נכנסות להזמנה/תווית/שיתוף | §8 |
| גרסאות: snapshot + `versionDiff` מחושב + שחזור לא-מוחק | §9 |
| הגנת `locked` על עריכה ועל שחזור | §9, §18.7 |
| מסך `diff` בין שני מתכונים | §9 |
| תפקידים ו-rank; גזירת UI לפי תפקיד | §10.1 |
| ברירת מחדל מגבילה להרשאות | §10.4 |
| כל היכולות המקצועיות: תשואה, פחת, עלות, פוד קוסט, אלרגנים, אחוזי אופה, הידרציה נטו, טמפ' מים, תת-מתכונים | §13 |
| `haccpOf` — 4 מצבים בסדר הנכון, נגזר ולא מאוחסן | §13a |
| Edge cases של אצוות (שדה ריק ≠ מדידה, אצווה ישנה = "לא תועד") | §13a |
| Cook Mode במלואו | §14 |
| RTL, `tabular-nums`, `direction:ltr` נקודתי | §15 |
| Design tokens מיושמים במדויק | §16 |
| המתנה לטעינת מודולים (80×60ms) + שגיאה מפורשת | §18.14 |
| באנר שגיאה בכשל שמירה, בלי קריסה | §18.13 |
| פחת אפייה ממדגם; בלי מדגם = 0 ולא ניחוש | §18.11 |
| מנגנון "לא נוצר כפל" בשמירת עותק | §11, §18.8 |

### PARTIAL — קיים אבל לא מלא
| פיצ'ר | מה חסר | סעיף |
|---|---|---|
| תלות ההמרות בכלי המדידה | עובד ב-`measure.js` בלבד. `engine.js` מקובע ל-240 מ"ל → **AC #5 נכשל** בכל המנוע | §18.3 |
| תג מקור הנתון בגלית ההמרה | הגלית מקבלת גרמים, ולכן מסמנת אומדן כ"מדויק" → **AC #2 נכשל** | §5.3 |
| טבלת צפיפויות | שלוש טבלאות שחולקות זו על זו | §5.1 |
| כיול אישי | התאמת substring דו-כיווני; לא נשמר נפח הכלי | §5.1, §1.2 |
| `fmt` לכלי מדידה | רבעים לכף/כפית במקום חצאים; "0 כוס" בכמות זעירה; "2 ביצהות" | §5.2 |
| ביטול נעילה | חד-כיווני, אין דרך לנעול מחדש | §9 |
| ייצוא JSON | הודעה בלבד, **וההודעה מטעה** | §17, AC #17 |
| שיתוף | קוד מקומי, **וההודעה מטעה** | §17, AC #17 |
| קטלוג חומרי גלם | השוואת שמות, לא ישות | §13 |
| הדפסה | CSS חושף רק `#orderSheet` | §16 |
| `PAN_KINDS` | קיימים בקוד, אבל בורר "התבנית שלי" מציע רק עגולה/מלבנית/GN — בלי מאפינס ואינגליש קייק | §7 |

### UI ONLY — קיים רק בממשק
| פיצ'ר | סעיף |
|---|---|
| קבוצות, קורסים, שיעורים — 2 קבוצות קבועות בקוד | §10.3, §17 |
| חברות — מספר בלבד | §17 |
| הזמנות, קוד קבוצה, קישור פרטי | §10.2 |
| בקשות ממתינות — 2 קבועות, אי אפשר לאשר | §17 |
| חמשת מתגי ההרשאות — משנים מסך בלבד | §10.4 |
| מתכון קבוצתי — נשלף מהמחברת האישית | §17, §11 |
| מודל פרטיות — מוצג ולא נאכף | §12 |
| ערבית — "בהכנה" | §15 |
| תיעוד מצולם — דגל בוליאני בלבד | §13a |

### MISSING — לא קיים
| פיצ'ר | סעיף |
|---|---|
| Authentication | Handoff §2 |
| Database ו-20 הטבלאות | Handoff §1 |
| כל 11 ה-endpoints/RPC | Handoff §4 |
| Storage לתמונות | Handoff §5 |
| סנכרון בין מכשירים | §17 |
| מצב סקיילינג "נפח סופי" — המפרט מסמן במפורש כחסר; `mlPerUnit` כבר קיים | §6 |
| תיקיית `images/` | README |
| מערך בדיקות ל-30+ ה-AC | §19 |
| התראה "יש גרסה חדשה בקבוצה" לעותק אישי — המפרט מסמן כהחלטה עתידית | §11 |
| נקודות בקרה פר-מטבח | §13a |
| i18n | §15 |

### NEEDS BACKEND
Auth · חברות והזמנות · קוד/קישור עם תוקף · אכיפת הרשאות · `save-copy` בשרת · RLS על מחברות והערות · סנכרון גרסאות · שיתוף חוץ-קבוצתי · מסד צפיפויות משותף · כיול פר-חשבון · העלאת צילומי אצווה · חותמת זמן מאומתת · נקודות בקרה פר-מטבח · proxy לפענוח החכם · locale פר-חשבון · ייצוא/ייבוא אמיתי.

### NEEDS SECURITY
| דרישה | סעיף |
|---|---|
| RLS: `private_notes` ו-`calibrations` — `user_id = auth.uid()` בלי יוצא מן הכלל | Handoff §3 |
| אין policy/view/RPC שמאפשר למדריך לקרוא מחברת או הערות של תלמיד | Handoff §3 — *"חוק שאין לעבור עליו"* |
| מפתח Claude API מעולם לא בדפדפן; proxy + rate limit | Handoff §6 |
| טוקני הזמנה חד-פעמיים, תוקף 7 ימים, נבדקים בשרת | Handoff §6 |
| קוד קבוצה יוצר **בקשה**, לא חברות | Handoff §6 |
| כל בדיקת הרשאה בממשק נאכפת שוב בשרת | Handoff §6 |
| מחיקת חשבון מוחקת `private_notes` ו-`calibrations` מיד | Handoff §6 |
| חותמת זמן של צילום שהמשתמש אינו יכול לערוך | §13a |
| ולידציה על קלט מספרי | לא במפרט — תוספת שלי |

### BUG
| # | תיאור | חומרה |
|---|---|---|
| B1 | `engine.js` מתעלם מ-`prefs.tools` → AC #5 נכשל | חמור |
| B2 | שלוש טבלאות צפיפות שחולקות זו על זו | חמור |
| B3 | גלית ההמרה מסמנת אומדן כ"מדויק" → AC #2 נכשל | חמור |
| B4 | התאמת כיול לפי substring דו-כיווני | חמור |
| B5 | כיול לא שומר את נפח הכלי | בינוני |
| B6 | `panFactor` משווה מאפינס לתבנית עגולה (26.18) | בינוני |
| B7 | ביטול נעילה חד-כיווני | בינוני |
| B8 | הודעות ייצוא/שיתוף מטעות → AC #17 נכשל | בינוני |
| B9 | `images/` חסרה + בקשות למחרוזות `{{ }}` | בינוני |
| B10 | "2 ביצהות"; "0 כוס" | קל |
| B11 | עיגול רבעים בכף/כפית במקום חצאים | קל |
| B12 | מחיקת אצווה לפי `code` + קודים כפולים | קל |
| B13 | שגיאת מעגל תת-מתכונים לא מוצגת | קל |
| B14 | קטלוג לפי שם מדויק; "מים" עם מחיר לק"ג | קל |
| B15 | מתכון קבוצתי נשלף מהמחברת האישית | קל |
| B16 | אין יחידות ספירה בגלית ההמרה | קל |
| B17 | טיימרים כפולים לאותו שלב | קל |

### סיכום Acceptance Criteria (§19)
**14 מתוך 17 עוברים.** נכשלים: **#2** (תג מקור שגוי — B3), **#5** (שינוי גודל כוס לא משפיע על המנוע — B1), **#17** (הודעות Mock מוצגות כמחוברות — B8).
AC #8–#11 לא ניתנים לאימות אמיתי בלי שרת — הם עוברים בשכבת ה-UI בלבד.

---

## מה קיבלנו מ-Claude Design — בשורה התחתונה

קיבלנו **פרוטוטייפ עיצובי באיכות גבוהה מאוד, עם מנוע חישוב מקצועי שעובד באמת**, ומפרט מסירה בן 659 שורות שכולל סכימת DB, policies של RLS ורשימת endpoints — כלומר חלק גדול מעבודת התכנון הארכיטקטונית כבר נעשה.

מה שלא קיבלנו: אפליקציה. אין זהות, אין מסד נתונים, אין שרת, ובדפדפן רגיל אין גם שמירה.

הדבר הכי יקר בחבילה הזאת הוא `engine.js` + `measure.js` — 434 שורות של ידע מקצועי בקונדיטוריה. שם גם שוכנים ארבעת הבאגים החמורים, וזה בדיוק המקום להתחיל.
